# FastAPI core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.0.0

# Database
asyncpg>=0.31.0

# HTTP client
httpx>=0.27.0

# Payments
yookassa>=3.10.0

# Aiogram (used for exception types in services/core/payment, services/core/referral)
aiogram>=3.23.0

# Logging
loguru>=0.7.0

# Background tasks
apscheduler>=3.10.0

# Retry logic (used in client.py XUISession)
tenacity>=8.0.0

# Circuit breaker
# Circuit breaker — provided by backend/circuit_breaker.py.
# The pybreaker package is incompatible with modern Tornado: its
# ``call_async`` references the removed ``tornado.gen`` module and
# raises NameError under Python 3.11 / Tornado >= 6.1.

# Prometheus metrics
prometheus-client>=0.20.0

# DI container (used in some service constructors)
punq>=0.6.1

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
